diff -Naru v1.8.0-1cd8a38f70.clean/src/util/thread.c v1.8.0-1cd8a38f70/src/util/thread.c
--- v1.8.0-1cd8a38f70.clean/src/util/thread.c	2024-03-20 21:19:37.000000000 +0100
+++ v1.8.0-1cd8a38f70/src/util/thread.c	2026-03-23 10:42:46.406938600 +0100
@@ -74,9 +74,10 @@
 
 int git_tlsdata_init(git_tlsdata_key *key, void (GIT_SYSTEM_CALL *destroy_fn)(void *))
 {
-	DWORD fls_index = FlsAlloc(destroy_fn);
+	DWORD fls_index = TlsAlloc();
+	(void)destroy_fn;
 
-	if (fls_index == FLS_OUT_OF_INDEXES)
+	if (fls_index == TLS_OUT_OF_INDEXES)
 		return -1;
 
 	*key = fls_index;
@@ -85,7 +86,7 @@
 
 int git_tlsdata_set(git_tlsdata_key key, void *value)
 {
-	if (!FlsSetValue(key, value))
+	if (!TlsSetValue(key, value))
 		return -1;
 
 	return 0;
@@ -93,12 +94,12 @@
 
 void *git_tlsdata_get(git_tlsdata_key key)
 {
-	return FlsGetValue(key);
+	return TlsGetValue(key);
 }
 
 int git_tlsdata_dispose(git_tlsdata_key key)
 {
-	if (!FlsFree(key))
+	if (!TlsFree(key))
 		return -1;
 
 	return 0;
diff -Naru v1.8.0-1cd8a38f70.clean/src/util/win32/thread.c v1.8.0-1cd8a38f70/src/util/win32/thread.c
--- v1.8.0-1cd8a38f70.clean/src/util/win32/thread.c	2024-03-20 21:19:37.000000000 +0100
+++ v1.8.0-1cd8a38f70/src/util/win32/thread.c	2026-03-23 10:37:48.170545300 +0100
@@ -18,7 +18,7 @@
 static win32_srwlock_fn win32_srwlock_acquire_exclusive;
 static win32_srwlock_fn win32_srwlock_release_exclusive;
 
-static DWORD fls_index;
+static DWORD tls_index;
 
 /* The thread procedure stub used to invoke the caller's procedure
  * and capture the return value for later collection. Windows will
@@ -29,7 +29,7 @@
 	git_thread *thread = lpParameter;
 
 	/* Set the current thread for `git_thread_exit` */
-	FlsSetValue(fls_index, thread);
+	TlsSetValue(tls_index, thread);
 
 	thread->result = thread->proc(thread->param);
 
@@ -38,7 +38,7 @@
 
 static void git_threads_global_shutdown(void)
 {
-	FlsFree(fls_index);
+	TlsFree(tls_index);
 }
 
 int git_threads_global_init(void)
@@ -58,7 +58,7 @@
 			GetProcAddress(hModule, "ReleaseSRWLockExclusive");
 	}
 
-	if ((fls_index = FlsAlloc(NULL)) == FLS_OUT_OF_INDEXES)
+	if ((tls_index = TlsAlloc()) == TLS_OUT_OF_INDEXES)
 		return -1;
 
 	return git_runtime_shutdown_register(git_threads_global_shutdown);
@@ -105,7 +105,7 @@
 
 void git_thread_exit(void *value)
 {
-	git_thread *thread = FlsGetValue(fls_index);
+	git_thread *thread = TlsGetValue(tls_index);
 
 	if (thread)
 		thread->result = value;
